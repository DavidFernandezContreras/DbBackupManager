I would like to build an ASP.net core MVC web application who has a hosted service in charge of backup a db from a postgresql remote server and restore this db overriding the previous db on another db server on a daily programmable basis. The specific time could be stored in the usual Webapp.config.json.
Mi concern here is to be able to display a list of the backup & restore operations done with associated success or fail info and the next scheduled backup and restore operation. I only really need a local file to store this simple backup&restore events and success o fail info associated to each of them. I would like also have the possibility to trigger one backup and restore operation just in time out of the scheduled time if needed.
By the way showing the db servers connectivity up to date info through a visual indicator would also be a plus, maybe we can use a separated hosted service for that.
